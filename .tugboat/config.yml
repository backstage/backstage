services:
  backstage:
    image: tugboatqa/node:lts
    expose: 7000
    default: true
    commands:
      build:
        - yarn install
        - yarn tsc
        - yarn build
      update:
        - yarn install
        - yarn tsc
        - yarn build
      start:
        - yarn start-backend &
