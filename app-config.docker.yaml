# To run local backstage instance with docker services,
# run:
# 1. `docker-compose up --wait`
# 2. `export BACKSTAGE_ENV=docker`
# 3. `yarn dev`
backend:
  database:
    client: pg
    connection:
      host: localhost
      port: 5432
      user: postgres
      password: postgres
  cache:
    store: redis
    connection: redis://localhost:6379

search:
  elasticsearch:
    provider: opensearch
    node: 'http://localhost:9200'
    auth:
      username: admin
      password: admin
