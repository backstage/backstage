import { ChangelogComponent } from '@/components/ChangelogComponent';
import { PageTitle } from '@/components/PageTitle';
import { PropsTable } from '@/components/PropsTable';
import { Snippet } from '@/components/Snippet';
import { useIsTruncatedReturnDefs } from './props-definition';
import { useIsTruncatedExampleSnippet } from './snippets';
import { UseIsTruncatedExample } from './components';

<PageTitle
  title="useIsTruncated"
  description="A hook to detect whether a text element is overflowing its container via CSS truncation."
/>

## Usage

Attach the `ref` to an element with `text-overflow: ellipsis`. The hook checks truncation on mount and re-checks on element resize (debounced), so `truncated` is always up-to-date.

<Snippet
  align="center"
  py={4}
  height={80}
  preview={<UseIsTruncatedExample />}
  code={useIsTruncatedExampleSnippet}
/>

## Return Value

<PropsTable data={useIsTruncatedReturnDefs} isHook />

<ChangelogComponent hook="use-is-truncated" />
