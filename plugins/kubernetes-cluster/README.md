# @backstage/plugin-kubernetes-cluster

This plugin provides a frontend view for Kubernetes cluster details in Backstage. It displays cluster overview information, node status, and API resources for entities of kind `Resource` with type `kubernetes-cluster`.

## Getting Started

See the [Kubernetes plugin documentation](https://backstage.io/docs/features/kubernetes/configuration) for installation and configuration instructions.
