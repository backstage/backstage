## API Report File for "@backstage/plugin-permission-node"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts
import { ConditionalPolicyDecision } from '@backstage/plugin-permission-common';
import { Conditions } from '@backstage/plugin-permission-node';
import { ExtensionPoint } from '@backstage/backend-plugin-api';
import { PermissionCondition } from '@backstage/plugin-permission-common';
import { PermissionCriteria } from '@backstage/plugin-permission-common';
import { PermissionPolicy } from '@backstage/plugin-permission-node';
import { PermissionResourceRef } from '@backstage/plugin-permission-node';
import { PermissionRule } from '@backstage/plugin-permission-node';
import { ResourcePermission } from '@backstage/plugin-permission-common';

// @alpha
export const adminConditions: Conditions<{
  isPlugin: PermissionRule<
    string,
    {
      pluginIds: string[];
    },
    'permission-plugin',
    {
      pluginIds: string[];
    }
  >;
}>;

// @alpha
export const adminPermissionResourceRef: PermissionResourceRef<
  string,
  {
    pluginIds: string[];
  },
  'permission-plugin',
  'permission'
>;

// @alpha
export const adminRules: {
  isPlugin: PermissionRule<
    string,
    {
      pluginIds: string[];
    },
    'permission-plugin',
    {
      pluginIds: string[];
    }
  >;
};

// @alpha
export const createAdminConditionalDecision: (
  permission: ResourcePermission<'permission-plugin'>,
  conditions: PermissionCriteria<PermissionCondition<'permission-plugin'>>,
) => ConditionalPolicyDecision;

// @alpha
export type PolicyExtensionPoint = {
  setPolicy(policy: PermissionPolicy): void;
};

// @alpha
export const policyExtensionPoint: ExtensionPoint<PolicyExtensionPoint>;

// (No @packageDocumentation comment for this package)
```
