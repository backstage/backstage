import { dfdsCapabilityPluginPlugin } from './plugin';

describe('dfds-capability-plugin', () => {
  it('should export plugin', () => {
    expect(dfdsCapabilityPluginPlugin).toBeDefined();
  });
});
