# Catalog Backend Module for the default System Entity Model

This is an extension module to the catalog backend plugin, providing the default System Entity Model (the default builtin kinds, relations etc).
