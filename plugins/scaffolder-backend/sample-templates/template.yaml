apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: test
  title: Test template
spec: 
  parameters:
    - title: Do something
      required:
        - myKey
      properties:
        myKey:
          title: My key
          description: asd
          type: string
          ui:field: Secret  
  steps: []
  type: service
    