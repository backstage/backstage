apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: test-secret-with-pattern
  title: test ui:field secret with pattern
  description: test-secret
spec:
  owner: test@example.com
  type: service
  parameters:
    - title: test secret with pattern
      required:
        - test
      properties:
        test:
          title: test secret with patatern
          type: string
          ui.autofocus: true
          ui:options:
            rows: 5
          ui:widget: password
          ui:field: Secret 
          maxLength: 8
          minLength: 8
          pattern: "^[a-z]{1}[a-z0-9]{2,7}$"

  steps:
    - id: dummy
      name: log
      action: debug:log
      input:
        message: done