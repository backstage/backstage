# Quickstart

1. Open the entity in Catalog.
2. Open the **Docs** tab.
3. Confirm this page renders under TechDocs.

If docs do not render, verify:

- `mkdocs.yml` exists next to `catalog-info.yaml`
- `backstage.io/techdocs-ref: dir:.` is present
- TechDocs backend is running
