# Sample TechDocs Local

This sample follows the recommended `dir:.` annotation model from the TechDocs guides.

## Why this sample exists

Use this entity to validate plain TechDocs generation and rendering when docs live next to `catalog-info.yaml`.

## Compare with editor sample

- `sample-docs-local`: `dir:.` (local generation flow)
- `sample-docs`: `url:` reference (editor-friendly flow)
