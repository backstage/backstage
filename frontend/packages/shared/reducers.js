import snackbar from 'shared/apis/snackbar/reducer';
import errorLog from 'shared/apis/errorLog/reducer';
import filters from 'shared/components/Filters/reducer';

export default {
  snackbar,
  errorLog,
  filters,
};
