# Connections Node

Internal Node.js service implementation for `@backstage/connections`. It loads
connections from configuration and adapts legacy `integrations` configuration.
This package is inlined into its consumers and is not published separately.
