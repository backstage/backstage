---
'@backstage/plugin-scaffolder': patch
---

Fixed an issue where the `Top Visited` and `Recently Visited` cards displayed a generic title (`Create a new component`) instead of the specific template name. Template titles are now correctly displayed (e.g., "My React App").
