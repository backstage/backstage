---
'@backstage/ui': patch
---

Added support for grouping options into sections in the Select component. Instead of regular options, you can now pass section objects with a `title` and a nested `options` array to render grouped dropdowns with section headers.

**Affected components:** Select
