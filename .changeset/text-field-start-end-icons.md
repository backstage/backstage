---
'@backstage/ui': patch
---

Added `iconStart` and `iconEnd` props to `TextField`, allowing arbitrary content to be placed before or after the input. The existing `icon` prop continues to work as an alias for `iconStart`.
