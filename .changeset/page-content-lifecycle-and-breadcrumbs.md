---
'@backstage/frontend-plugin-api': patch
---

Apply plugin providers and analytics once per page content boundary, keeping page headers visible during loading and errors. Sub-page breadcrumbs now point to their matched route even when rendered inside nested page layouts.
