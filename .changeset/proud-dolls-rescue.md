---
'@backstage/backend-defaults': minor
---

Introduce `discoveryFeatureLoaderFactory`, which accepts a list of packages that should never be dynamically loaded.

This aims to make it easier to adopt the `discoveryFeatureLoader`, see XXX documentation.
