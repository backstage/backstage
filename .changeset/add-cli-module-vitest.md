---
'@backstage/cli-module-vitest': minor
---

Added a new `@backstage/cli-module-vitest` package that provides Vitest-based `repo test` and `package test` commands as an alternative to the default Jest-based test runner. To switch to Vitest, add `@backstage/cli-module-vitest` and `vitest` as dependencies in your project root.
