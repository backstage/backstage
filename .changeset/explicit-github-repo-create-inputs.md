---
'@backstage/plugin-scaffolder-backend-module-github': minor
---

**BREAKING**: Explicitly defined the supported inputs in the `github:repo:create` action schema to ensure properties handled by the action are accepted.
