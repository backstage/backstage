---
'@backstage/plugin-scaffolder-backend-module-github': patch
---

Explicitly defined the supported inputs in the `github:repo:create` action schema to ensure properties handled by the action are accepted.
