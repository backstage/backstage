---
'@backstage/plugin-catalog-backend-module-ai-model': patch
---

Added registration of the `ai-model-server` API spec type alongside the existing `AiResource` and `mcp-server` catalog model layers.
