---
'@backstage/backend-plugin-api': minor
---

Added a new `KeyValueStoreService` core service and `coreServices.keyValueStore` service ref, providing backend plugins with a persistent, schema-validated key-value store backed by the plugin's own database.
