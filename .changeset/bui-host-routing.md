---
'@backstage/core-app-api': patch
'@backstage/plugin-app': patch
---

Configured BUI's explicit routing integration so its links and collections use client-side navigation at their consuming route scope. The new frontend app uses the same scoped routing API available to plugins integrating React Aria directly.
