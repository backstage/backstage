---
'@backstage/plugin-kubernetes-react': patch
---

Improved Kubernetes pod delete and exec terminal error messaging. Permission denials and connection failures now appear directly in the UI instead of leaving users on a loading state or pointing them to the browser console.
