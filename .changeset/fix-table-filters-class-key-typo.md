---
'@backstage/core-components': minor
---

**BREAKING** Fixed typo in `TableFiltersClassKey`: the `'heder'` literal has been corrected to `'header'`. Adopters using this type to reference the CSS class key by name will need to update their usage.
