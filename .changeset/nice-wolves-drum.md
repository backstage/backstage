---
'@backstage/ui': patch
---

Add new `iconStart` + `iconEnd` props to `Accordion` component in Backstage UI

Add an icon to improve visual hierarchy and scannability. Icons can be positioned at the start (default) or end of the title.

**Affected components:** Accordion
