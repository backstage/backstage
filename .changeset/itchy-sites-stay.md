---
'@backstage/plugin-api-docs': patch
---

Updated `DefaultApiExplorerPage` to use the new CatalogTable API with `ColumnConfig` from `@backstage/ui` instead of `TableColumn` from `@backstage/core-components`.
