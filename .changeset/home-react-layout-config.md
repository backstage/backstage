---
'@backstage/plugin-home-react': patch
---

Added `HomePageDefaultConfigItem` type and optional `defaultConfig` prop to `HomePageLayoutProps`, allowing home page layouts to receive a default grid configuration from the app config.
