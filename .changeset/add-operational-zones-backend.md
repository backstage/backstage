---
'@backstage/plugin-operational-zones-backend': patch
---

Added the `@backstage/plugin-operational-zones-backend` plugin with REST API endpoints (`GET /zones`, `GET /zones/:operationId`, `POST /zones`) and a 1-minute scheduler heartbeat for evaluating operational zone levels.
