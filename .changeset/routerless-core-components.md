---
'@backstage/core-components': patch
---

Links, query controls, sign-in redirects, and log selection use Backstage app history when available. These components can render without a page router while retaining legacy frontend support and native browser link behavior.
