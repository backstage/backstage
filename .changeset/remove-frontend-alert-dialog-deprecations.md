---
'@backstage/frontend-plugin-api': minor
---

Removed the deprecated alert API exports and the deprecated `show` and `showModal` dialog methods. Use `ToastApi` for notifications and `DialogApi.open` for dialogs.
