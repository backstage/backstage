---
'@backstage/config-loader': minor
'@backstage/backend-defaults': patch
---

Added `enumerateConfigSecrets` for identifying string values marked secret by the loaded configuration schema, and aligned backend configuration secret enumeration with this shared API.
