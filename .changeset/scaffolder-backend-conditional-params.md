---
'@backstage/plugin-scaffolder-backend': patch
---

The parameter schema endpoint now passes through the `if` field from parameter step entries, enabling conditional step visibility in the scaffolder wizard frontend.
