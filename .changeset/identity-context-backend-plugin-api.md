---
'@backstage/backend-plugin-api': minor
---

Added optional verified identity context to Backstage user principals. Routes can inspect an issuer and a bounded map of issuer-defined string attributes without a separate identity lookup.
