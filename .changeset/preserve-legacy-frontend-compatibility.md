---
'@backstage/core-plugin-api': patch
---

Made the legacy alert, icon component, and `withApis` compatibility APIs independent of `@backstage/frontend-plugin-api` so that they remain available to legacy frontend apps.
