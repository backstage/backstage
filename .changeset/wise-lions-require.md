---
'@backstage/plugin-scaffolder-backend-module-azure': patch
---

Added support for requiring user-provided credentials for Azure DevOps mutation actions when `scaffolder.requireScmUserCredentials` is enabled.
