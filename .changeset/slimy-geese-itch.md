---
'@backstage/plugin-scaffolder': minor
---

Added namespace filter for MyGroupsPicker. Users can now define the namespaces that they want for groups listed for users. This ensures if you have different groups relevant to different templates and they are namespaced, they can be filtered using this option.
