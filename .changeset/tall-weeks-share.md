---
'@backstage/core-compat-api': patch
---

Fixed analytics context plugin IDs for legacy entity-page tabs in hybrid apps, so events are associated with the contributing plugin instead of always using the Catalog plugin context.
