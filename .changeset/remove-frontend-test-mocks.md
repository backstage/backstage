---
'@backstage/frontend-test-utils': minor
---

Removed the deprecated standalone mock API classes and option types. Use the corresponding helpers in `mockApis`; `MockFetchApiOptions` remains available for configuring `mockApis.fetch`.
