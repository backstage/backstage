---
'@backstage/backend-app-api': patch
---

Added an implementation for the new `RedactionsService`. This also moves the reading of config secrets for redactions from the root logger service to the root config service.
