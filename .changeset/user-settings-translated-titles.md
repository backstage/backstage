---
'@backstage/plugin-user-settings': patch
---

The Settings page title and its tab titles (General, Authentication Providers, Feature Flags) are now translated when using the new frontend system, matching the app's selected language instead of always showing English.
