---
'@backstage/backend-defaults': patch
---

Verify and preserve identity context in user credentials across limited-user and on-behalf-of plugin tokens. Upgrade backend verifiers and delegation services before enabling context issuance.
