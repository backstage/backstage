---
'@backstage/plugin-catalog-incremental-ingestion': patch
---

Added a catalog incremental ingestion frontend plugin that contributes an `Incremental Ingestion` DevTools tab for apps using the new frontend system.
