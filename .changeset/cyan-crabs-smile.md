---
'@backstage/ui': patch
---

Feat: Add Breadcrumbs component & useIsTruncated hook

Added `Breadcrumbs` and `Breadcrumb` components for rendering breadcrumb navigation with automatic truncation, tooltips, and keyboard accessibility. Also added `useIsTruncated` hook for detecting CSS text overflow.
