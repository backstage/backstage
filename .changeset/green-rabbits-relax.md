---
'@backstage/plugin-catalog-backend-module-incremental-ingestion': patch
---

Fixed initialization so the incremental ingestion admin routes are ready when the catalog backend starts. Improved the module documentation with updated admin route behavior, a backend state guide, and example provider wiring.
