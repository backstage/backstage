---
'@backstage/cli-defaults': patch
---

Updated the package documentation to clarify that the default CLI module aggregate must be installed directly in the target repository and can be combined with individual command modules.
