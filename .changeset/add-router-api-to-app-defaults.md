---
'@backstage/app-defaults': patch
---

Added a default `RouterApi` implementation to the old frontend system, enabling packages that use routing hooks from `@backstage/frontend-plugin-api` to work in legacy apps.
