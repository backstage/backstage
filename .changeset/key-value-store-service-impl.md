---
'@backstage/backend-defaults': minor
'@backstage/backend-test-utils': minor
---

Added the default factory implementation for the new `KeyValueStoreService`, including database migrations and a `DefaultKeyValueStoreService` class. Added `mockServices.keyValueStore` for testing.
