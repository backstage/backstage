---
'@backstage/plugin-catalog': minor
---

Added `home-page-widget:catalog/starred-entities` extension to the catalog alpha plugin. The widget, moved from `@backstage/plugin-home`, displays a user's starred entities and can optionally be grouped by kind.
