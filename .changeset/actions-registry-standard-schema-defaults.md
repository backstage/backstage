---
'@backstage/backend-defaults': patch
---

Updated the Actions Registry implementation to validate direct Standard Schema values, including asynchronous validation and transformations, and to publish draft-07 metadata through Standard JSON Schema conversion.
