---
'@backstage/plugin-catalog-node': patch
---

Added `provideCatalogModelDiscovery` to the alpha entrypoint, which discovers and registers catalog model layers from packages with the `catalog-model-layer` role.
